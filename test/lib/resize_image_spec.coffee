expect = require('chai').expect

describe "ResizeImage", ->
  it "accepts an image stream and resize it to wanted dimension", (done) ->
  it "produces equal or smaller size of the image", (done) ->
  it "converts gif to jpeg to disable animation", (done) ->
