expect     = require('chai').expect
ProxyImage = require('../../lib/proxy_image')

describe "ProxyImage", ->
  it "accepts an image stream and proxy the response back to the viewer", (done) ->
  it "returns 400 error response when the image stream is not accessible", (done) ->
