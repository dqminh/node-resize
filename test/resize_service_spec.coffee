#ResizeService = require('../lib/resize_service').ResizeService

describe "ResizeService", ->
  it "returns empty response with status 400 when size is (null)"
  it "fails when size contain 0 as one of the dimension"
  it "resizes an image"

